% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_basin.R
\name{plot_basin_point_feature}
\alias{plot_basin_point_feature}
\title{Add sized and colored site markers to an existing map}
\usage{
plot_basin_point_feature(
  p,
  data,
  x_col,
  y_col,
  fill_col,
  size_col,
  label_col = NULL,
  facet_col = NULL,
  fill_mode = c("manual", "continuous"),
  fill_colors = NULL,
  fill_title = NULL,
  size_range = c(2, 6),
  size_breaks = NULL,
  size_limits = NULL,
  size_title = NULL,
  facet_nrow = NULL,
  show_labels = TRUE,
  style = point_feature_style()
)
}
\arguments{
\item{p}{A `ggplot` object to add layers to (e.g., from `plot_basin_base()`).}

\item{data}{A non-empty `data.frame` with site-level data.}

\item{x_col}{Column with x-coordinates (longitude).}

\item{y_col}{Column with y-coordinates (latitude).}

\item{fill_col}{Column for point fill color.}

\item{size_col}{Numeric column for point size.}

\item{label_col}{Optional column for site labels. `NULL` = no labels.}

\item{facet_col}{Optional column for faceting. `NULL` = no faceting.}

\item{fill_mode}{One of `"manual"` or `"continuous"`.}

\item{fill_colors}{Named color vector (manual) or >= 2 color vector
(continuous). `NULL` uses defaults.}

\item{fill_title}{Legend title for fill. `NULL` uses column name.}

\item{size_range}{Numeric length-2, min/max point sizes. Default `c(2, 6)`.}

\item{size_breaks}{Numeric vector of size legend breaks. `NULL` = auto.}

\item{size_limits}{Numeric length-2, size scale limits. `NULL` = auto.}

\item{size_title}{Legend title for size. `NULL` uses column name.}

\item{facet_nrow}{Integer or `NULL`. Rows for `facet_wrap()`.}

\item{show_labels}{Logical. Draw labels? Default `TRUE`.}

\item{style}{A `point_feature_style()` object.}
}
\value{
A `ggplot2` plot object.
}
\description{
Takes an existing `ggplot` object (typically from `plot_basin_base()`) and
layers on site points with fill and size encodings, optional labels, and
optional faceting. The base-map sf layers are repeated across facet panels
automatically by ggplot2.
}
\details{
Column arguments (`x_col`, `y_col`, `fill_col`, `size_col`, `label_col`,
`facet_col`) accept either unquoted column names or single character strings.

**Fill modes:**
\itemize{
  \item `"manual"` (default): `fill_colors` is a named character vector
    passed to `scale_fill_manual(drop = FALSE)`.
  \item `"continuous"`: `fill_col` must be numeric; `fill_colors` (>= 2
    colors) defines the gradient via `scale_fill_gradientn()`.
}
}
\examples{
\dontrun{
p_base <- plot_basin_base(
  basins_sf = basins_sf,
  rivers_sf = rivers_sf,
  min_stream_order = 3
)

plot_basin_point_feature(
  p         = p_base,
  data      = ggs,
  x_col     = x,
  y_col     = y,
  fill_col  = `Risk Level`,
  size_col  = perc_outside,
  label_col = site,
  facet_col = scenario,
  fill_colors = c("No change" = "#009E73", "Low" = "#F0E442",
                   "Medium" = "#E69F00", "High" = "#C00000"),
  fill_title  = "Risk Level",
  size_range  = c(2, 6),
  size_breaks = seq(30, 60, 10),
  size_limits = c(30, 60),
  size_title  = "Metrics\noutside (\%)",
  facet_nrow  = 1
)
}

}
